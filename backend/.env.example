# Server
PORT=5000
NODE_ENV=development

# ─── Database (Local or Azure Database for PostgreSQL) ──────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=azure_cost_monitor
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL=false

# ─── JWT (used for local auth; ignored when AZURE_AD_ENABLED=true)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# ─── Azure Identity ────────────────────────────────────────
# Register an App in Azure AD > Certificates & secrets > New client secret
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret

# ─── Azure Subscriptions (comma-separated for multi-sub) ───
AZURE_SUBSCRIPTION_IDS=sub-id-1,sub-id-2

# ─── Mode: set to false to use REAL Azure APIs ─────────────
AZURE_USE_MOCK=true

# ─── Azure AD Authentication ───────────────────────────────
AZURE_AD_ENABLED=false
AZURE_AD_AUDIENCE=api://your-client-id

# ─── Data Sync Schedule (cron expression) ──────────────────
SYNC_CRON=0 */6 * * *
SYNC_COST_DAYS=30

# ─── Notifications ─────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SLACK_WEBHOOK_URL=

# ─── Frontend URL ──────────────────────────────────────────
FRONTEND_URL=http://localhost:5173
